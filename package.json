{
  "name": "activitywatch-api",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "version": "0.0.4",
  "scripts":{
    "build": "bunx swagger-typescript-api -p app/aw/spec.yaml -o app/aw --api-class-name ActivityWatchAPI --route-types -r -d --unwrap-response-data"
  }
}
